/*
	category: mooTools
	name: adjDrops
	toolTip: Array de Configuracion de array de elementos Drags

	toolbar: true
	icon: com.aptana.ide.snippets/icons/comment_block.png
	language: text/css
	
	prompt(nombre): nombre de Array
	prompt(cantidad): cantidad de elementos
	prompt(x): cantidad de elementos
	prompt(y): cantidad de elementos
*/

	// Array ${nombre}
	var ${nombre} = new Array (${cantidad});
	${nombre} = [
		{x: ${x}, y: ${y}},
	];